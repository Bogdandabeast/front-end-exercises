// Camel case → userName

//Pascal case → UserName

//Snake case → user_name

//Kebab case → user-name

var caca = "no usar";

let mutable = "esto podra ser cambiado";

const impermeable = "no se altera";